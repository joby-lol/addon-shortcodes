site:
  url: 'http://leafcutter.test/addon-shortcodes/demo/'

theme:
  activate:
    - core/basic

addons:
  activate:
    - shortcodes

templates:
  site:
    name: Demo Leafcutter site

cache:
  driver: ephemeral
  directory: '${base_dir}/cache/'

assets:
  output:
    directory: '${base_dir}/assets/'
    url: '@/assets/'
